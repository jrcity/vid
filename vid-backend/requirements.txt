fastapi
uvicorn[standard]
pydantic
pydantic-settings
phonenumbers
httpx
python-dotenv
network-as-code
qrcode[pil]
Pillow 
slowapi # for endpoints rate limit security
pytest
pytest-asyncio
scikit-learn
joblib
numpy 
# SIM Farming Detection Dependencies with respective versions, incase our old python stable release rejects our new dependencies.  
# numpy==1.24.3 # for now I'll desable this one with the version since you guys have already placed one.
pandas==2.0.3
# scikit-learn==1.3.0 # Same applies to the numpy guys.
joblib==1.3.2
networkx==3.1 # Graph processing for digital twin
aiocache==0.12.2 # Async and performance, very important this one guys
# redis==5.0.0  # Optionally this one, disabled it by default Gobes, enable only if you want Redis caching!
joblib==1.3.2 # Then here we check the ML Model serialization

## I love, putting versions as a good refference of my deployment depndecies.  
